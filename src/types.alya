# Types and data structures for http
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct HttpConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return HttpConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
